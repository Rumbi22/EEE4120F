function out = parr_bubble()
    num = 10; %vary this during testing
    rand_num = rand(num);
    
    tic()
    %display(rand_num)
    
    parfor col = 1:num
        temp_column = rand_num(:,col)
       for row = 1:num
           
     %       display(rand_num(row,col))
        
       end
     
    end
    toc()

    %disp(out);
end
